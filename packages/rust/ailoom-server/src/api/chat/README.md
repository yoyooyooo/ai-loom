# Chat API (Codex bridge)

This folder will contain REST endpoints for:
- new (create conversation)
- resume (resume last or by path)
- send (send user message)
- interrupt (stop generation)
- utils (error/status mapping)

